#include "main.h"

/**
 * add - a function that adds two integers
 * and returns the result.
 *
 * @a: int type number
 * @b: int type number
 *
 * Return: Result of the addition of the two numbers
 */
int add(int a, int b)
{
return (a + b);
}
